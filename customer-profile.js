(() => {'use strict';const $=s=>document.querySelector(s);function read(k,f){try{const v=localStorage.getItem(k);return v?JSON.parse(v):f}catch{return f}}function write(k,v){try{localStorage.setItem(k,JSON.stringify(v));return true}catch{return false}}const user=read('classic-mart-user',null)||{},session=read('classic-mart-session',null)||{};let profile={name:user.name||session.name||'Classic Mart Member',email:user.email||session.email||'member@classicmart.test',phone:user.phone||session.phone||'+256 700 000 000',role:user.role||session.role||'customer',roleLabel:user.roleLabel||session.roleLabel||'Customer',roleProfile:user.roleProfile||{}};const initials=profile.name.split(/\s+/).filter(Boolean).slice(0,2).map(x=>x[0]).join('').toUpperCase()||'CM';$('#customerAvatar').textContent=initials;$('#customerName').textContent=profile.name;$('#customerRole').textContent=`${profile.roleLabel} account`;$('#customerContact').textContent=`${profile.email} · ${profile.phone}`;function drawDetails(){const d=profile.roleProfile||{};$('#accountDetails').innerHTML=`<div><span>Full name</span><strong>${profile.name}</strong></div><div><span>Email address</span><strong>${profile.email}</strong></div><div><span>Phone number</span><strong>${profile.phone}</strong></div><div><span>Account role</span><strong>${profile.roleLabel}</strong></div><div><span>Delivery location</span><strong>${d.location||'Not added yet'}</strong></div><div><span>Member since</span><strong>${user.createdAt?new Date(user.createdAt).toLocaleDateString(undefined,{month:'long',year:'numeric'}):'July 2026'}</strong></div>`;$('#profileLocation').textContent=d.location||'Add delivery location';$('#profileFocus').textContent=d.focus||'Best value and deals'}drawDetails();const catalog=[{id:1,name:'Wireless Earbuds',price:29.99,image:'https://images.unsplash.com/photo-1606220945770-b5b6c2c55bf1?auto=format&fit=crop&w=600&q=82'},{id:3,name:'Smart Watch',price:89.99,image:'https://images.unsplash.com/photo-1523275335684-37898b6baf30?auto=format&fit=crop&w=600&q=82'},{id:5,name:"Men's Sneakers",price:49.99,image:'https://images.unsplash.com/photo-1542291026-7eec264c27ff?auto=format&fit=crop&w=600&q=82'},{id:10,name:'Noise Cancelling Headphones',price:89.99,image:'https://images.unsplash.com/photo-1505740420928-5e560c06d30e?auto=format&fit=crop&w=600&q=82'}];const saved=read('shophub-wishlist',[]).map(Number);$('#profileWishlistCount').textContent=String(saved.length);const picks=(saved.map(id=>catalog.find(p=>p.id===id)).filter(Boolean).concat(catalog)).filter((p,i,a)=>a.findIndex(x=>x.id===p.id)===i).slice(0,3);$('#profileWishlistPreview').innerHTML=picks.map(p=>`<a class="profile-wishlist-item" href="index.html?product=${p.id}#trending"><img src="${p.image}" alt="${p.name}"><div><strong>${p.name}</strong><small>$${p.price.toFixed(2)}</small></div></a>`).join('');const modal=$('#profileEditModal');function open(){ $('#editName').value=profile.name;$('#editEmail').value=profile.email;$('#editPhone').value=profile.phone;$('#editLocation').value=profile.roleProfile.location||'';modal.hidden=false;document.body.style.overflow='hidden'}function close(){modal.hidden=true;document.body.style.overflow=''}$('#editProfileButton').onclick=open;document.querySelector('[data-edit-profile]').onclick=open;$('#profileEditClose').onclick=close;modal.addEventListener('click',e=>{if(e.target===modal)close()});$('#profileEditForm').addEventListener('submit',e=>{e.preventDefault();profile.name=$('#editName').value.trim();profile.email=$('#editEmail').value.trim();profile.phone=$('#editPhone').value.trim();profile.roleProfile={...profile.roleProfile,location:$('#editLocation').value.trim()};const updated={...user,...profile};write('classic-mart-user',updated);write('classic-mart-session',{...session,name:profile.name,email:profile.email,phone:profile.phone,role:profile.role,roleLabel:profile.roleLabel});location.reload()});$('#managePreferences').onclick=()=>location.href='onboarding.html';})();